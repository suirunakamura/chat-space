.side-bar
  .side-bar__userlist
    .side-bar__userlist--info
      .side-bar__userlist--info--name masa
      .side-bar__userlist--info--icons
        = link_to '/groups/new', class: "icon_1" do
          = icon('far', 'edit')
        = link_to "/users/:id", class: "icon_2" do
          = icon('fas', 'cog')
  .side-bar__group
    - current_user.groups.each do |group|
      .side-bar__group__box
        = link_to '#' do
          .side-bar__group__box--name
            =  group.name
          .side-bar__group__box--messages
            メッセージはまだありません。